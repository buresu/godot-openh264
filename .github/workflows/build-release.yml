name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build (${{ matrix.os }}, ${{ matrix.arch }}, ${{ matrix.config }})
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        include:
          - os: windows
            arch: x86_64
            config: Debug
            runner: windows-latest
            cmake_args: -A x64
            ext: dll
          - os: windows
            arch: x86_64
            config: Release
            runner: windows-latest
            cmake_args: -A x64
            ext: dll
          - os: windows
            arch: arm64
            config: Debug
            runner: windows-latest
            cmake_args: -A ARM64
            ext: dll
          - os: windows
            arch: arm64
            config: Release
            runner: windows-latest
            cmake_args: -A ARM64
            ext: dll
          - os: macos
            arch: arm64
            config: Debug
            runner: macos-latest
            cmake_args: ''
            ext: dylib
          - os: macos
            arch: arm64
            config: Release
            runner: macos-latest
            cmake_args: ''
            ext: dylib
          - os: linux
            arch: x86_64
            config: Debug
            runner: ubuntu-latest
            cmake_args: ''
            ext: so
          - os: linux
            arch: x86_64
            config: Release
            runner: ubuntu-latest
            cmake_args: ''
            ext: so
          - os: linux
            arch: arm64
            config: Debug
            runner: ubuntu-24.04-arm
            cmake_args: ''
            ext: so
          - os: linux
            arch: arm64
            config: Release
            runner: ubuntu-24.04-arm
            cmake_args: ''
            ext: so

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Configure CMake
        run: cmake -B build ${{ matrix.cmake_args }} -DCMAKE_BUILD_TYPE=${{ matrix.config }}

      - name: Build
        run: cmake --build build --config ${{ matrix.config }}

      - name: Install
        run: cmake --install build --config ${{ matrix.config }}

      - name: Get library filename
        id: lib-name
        shell: bash
        run: |
          config_lower=$(echo "${{ matrix.config }}" | tr '[:upper:]' '[:lower:]')
          echo "name=libgodot-openh264.${{ matrix.os }}.${config_lower}.${{ matrix.arch }}.${{ matrix.ext }}" >> $GITHUB_OUTPUT

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: lib-${{ matrix.os }}-${{ matrix.arch }}-${{ matrix.config }}
          path: demo/addons/godot-openh264/bin/${{ steps.lib-name.outputs.name }}

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Prepare release package
        run: |
          mkdir -p release/addons/godot-openh264/bin
          cp artifacts/* release/addons/godot-openh264/bin/
          cp demo/addons/godot-openh264/godot-openh264.gdextension release/addons/godot-openh264/
          cp demo/addons/godot-openh264/godot-openh264.gdextension.uid release/addons/godot-openh264/
          cd release
          zip -r ../godot-openh264-${{ github.ref_name }}.zip .

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: godot-openh264-${{ github.ref_name }}.zip
          generate_release_notes: true